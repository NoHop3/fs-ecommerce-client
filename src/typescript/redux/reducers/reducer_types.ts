export type InitialNavState = {
  navClass: string;
};

export type InitialToggleState = {
  lamp: boolean;
};

export type InitialAuthState = {
  authToken: string;
  isLoggedIn: boolean;
  isInSignIn: boolean;
};